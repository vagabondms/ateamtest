import styled from 'styled-components';
import { Button } from 'antd';

export const StyledButton = styled(Button)`
	width: 100px;
	height: 32px;
	font-weight: 500;
	font-size: 12px;
	margin-right: 8px;
	border-radius: 4px;
`;
